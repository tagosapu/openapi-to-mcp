

## プロジェクト概要 (Project Overview)

## general rules
- 日本語で回答
- .envは直接読み込まず、`python-dotenv`などのライブラリを使用して環境変数を読み込む
- 環境変数は出力しないようにする

## アーキテクチャと技術スタック (Architecture & Technical Stack)


### Python環境構築
- **必須**: `uv` をPython環境管理に使用（pip、conda、virtualenvは使用禁止）
- **ワークスペース構成**: ルート + langgraph_workflow サブプロジェクト
- 環境構築コマンド:
  ```bash
  uv init
  uv add <package>
  uv run <command>
  ```

### 開発ワークフロー
- **TDD実践中**: 
- **Git Flow**ブランチ運用
- テスト通過後のみコミット可能
- 開発サイクル: ブランチ作成 → テスト作成 → コード実装 → テスト実行 → 全クリア後コミット → マージ

#### TDD原則
- **重要**: テストが失敗した際は、テストコードではなく実装コードを修正してテストをクリアする
- テストコードは仕様書として機能するため、正しい期待値を定義する
- 実装コードがテストに合わせて修正されることで、品質と仕様の一貫性を保つ
- 例外: テストケース自体に明らかな論理エラーがある場合のみテストを修正可能
- 機能追加した際はテストカバレッジを確認し、適宜テストコードに追加する

## フォルダ構造とコンテンツ生成ワークフロー (Folder Structure & Content Generation)
